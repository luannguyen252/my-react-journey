import React, { PureComponent } from "react";

export default class Header extends PureComponent {
  render() {
    return (
      <header id="header">
        <h1>React Router Dynamic Routes</h1>
      </header>
    );
  }
}
